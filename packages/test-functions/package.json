{
  "name": "test-functions",
  "version": "2.0.0",
  "private": true,
  "description": "Sample Azure Functions app demonstrating @apvee/azure-functions-openapi v2.x",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "lint": "echo \"No lint configured for sample\"",
    "test": "echo \"No tests yet for sample\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@apvee/azure-functions-openapi": "file:../azure-functions-openapi",
    "@azure/functions": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@ngneat/falso": "^8.0.2",
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
